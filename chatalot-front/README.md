### Collabora Frontend Project

- To build the project run the following commands in the project root (this directory):
  - npm install
  - bower install
  - grunt build

- To run locally run "grunt serve"
